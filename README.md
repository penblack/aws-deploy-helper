# aws-deploy-helper

Automation scripts for deploying services to AWS EC2 and ECS.

## Setup

```bash
cp .env.example .env
# fill in your credentials
./deploy.sh staging
```

## Requirements

- AWS CLI v2
- Terraform >= 1.3
- bash >= 4
